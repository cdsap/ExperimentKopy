version: 2.1

jobs:
  build:
    docker:
      - image: cimg/android:2025.02
    environment:
      PR_CI_BUILD: true
    steps:
      - checkout
      - run:
          name: Build with Gradle
          command: |
            cat /proc/meminfo  
            ./gradlew :app:assembleDebug
            cat /proc/meminfo

workflows:
  pr_build:
    jobs:
      - build:
          filters:
            branches:
              only:
                - circlecli
